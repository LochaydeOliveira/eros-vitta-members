RewriteEngine On
RewriteBase /api/

# Arquivos/pastas reais passam direto
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Demais requests vão para index.php (mantém /api/health, /api/auth/*, etc)
RewriteRule ^ index.php [L,QSA]


